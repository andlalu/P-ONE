[pytest]
addopts = -m "not production_integration"
markers =
    slow: numerical benchmark tests that run Monte Carlo simulations
    production_integration: full production numerical tests run only when explicitly selected
